services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: rsv-helmet-frontend

    ports:
      - "5173:5173"

    volumes:
      - .:/app
      - /app/node_modules

    environment:
      CHOKIDAR_USEPOLLING: "true"

    restart: unless-stopped